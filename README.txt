
📖 Інструкція:

1️⃣ Встановити залежності:
pip install -r requirements.txt

2️⃣ Встановити ffmpeg:
sudo apt update
sudo apt install ffmpeg

3️⃣ Перевірити версію ffmpeg:
ffmpeg -version

4️⃣ Запустити бота:
python3 bot.py

Команди:
/start — почати
/delete — видалити відео користувача
